doctype
html
	head
		meta(charset='utf-8')
	body.tundra
		#root
		div#share-modal.modal-wrapper.hidden
			
		script(id='library-load' src='https://alpha.blueraster.io/gfw-mapbuilder/246-whats-good/1.3.4.js')
		script.
			console.log('%META_VERSION%')
			console.log('%RESOURCES_JSON%');

			var libLoad = document.getElementById('library-load');
			var metaVersion = '%META_VERSION%';
			metaVersion = metaVersion.substring(0, metaVersion.length - 1);
			metaVersion = metaVersion.substring(1, metaVersion.length);
			console.log(libLoad);
			console.log(libLoad.setAttribute)
			libLoad.setAttribute('version', metaVersion);
			console.log('after set')

		script.
			var grabLukeConfig = JSON.parse('%RESOURCES_JSON%');
			console.log(grabLukeConfig);
			var customApp = new MapBuilder({
				el: 'root',
				config: grabLukeConfig
			});
